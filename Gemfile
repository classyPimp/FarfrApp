source 'https://rubygems.org'
#!~paginator
gem 'will_paginate'
# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.0'
# Use sqlite3 as the database for Active Record
gem 'sqlite3'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
#gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Use ActiveModel has_secure_password
gem 'bcrypt', '~> 3.1.7'

# Use Unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

group :development, :test do
   # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'

  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'
  #!~
  gem 'better_errors'
  gem 'binding_of_caller'
  gem 'meta_request'
  

end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
##!!no need for regex email valiation
gem 'email_validator'
##!! bootstrap
gem "twitter-bootstrap-rails"
##!! fileuploads handler paperclip !! if raises content mismatch with type error goto: githubpaperclip for installing file.exe instructions
gem "paperclip", "~> 4.2"
##!! jquery jquery chosen #TODO: ypu probaly don't need this
gem "chosen-rails"
##!! ryan nested form. Adds/removes dynamically fields in forms
gem "nested_form"
##!! formtastic. Better forms.
gem "formtastic"
##!! formtastic styles to bootstrap
gem 'formtastic-bootstrap'
##!! HAML
gem 'haml'
#PDF generator HTML to PDF#REMINDER alter path in wicked_pdf.rb in production
gem 'wicked_pdf'
#dependency for wicked_pdf
gem 'wkhtmltopdf-binary'
#remote true file uploads
gem 'remotipart'
#observer pattern gem
gem 'wisper'
#searching nonfulltext gem
gem 'ransack'

#gem 'jquery-turbolinks'